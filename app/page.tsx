import Link from "next/link";
import { PageShell } from "@/components/PageShell";
import { AffiliateButton } from "@/components/AffiliateButton";
import { FAQSection } from "@/components/StructuredData";
import { pageFaqs } from "@/lib/faq";
import { pageMetadata } from "@/lib/seo";

export const metadata = pageMetadata("Firecrawl Tools for Developers", "Independent Firecrawl tools and practical guides for URL to Markdown, crawling, extraction, API, MCP, pricing, and larger web data workflows.", "/");

const tools = [
  ["URL to Markdown", "Turn one public webpage into clean Markdown.", "/tools/url-to-markdown"],
  ["Website to Markdown", "Select a handful of sitemap URLs and export them.", "/tools/website-to-markdown"],
  ["llms.txt generator", "Create a starter discovery file from your sitemap.", "/tools/llms-txt-generator"],
  ["URL extractor", "See internal and external links in HTML or a URL.", "/tools/url-extractor"],
] as const;

export default function Home() {
  return <PageShell><section className="home-hero"><div className="container home-hero-grid"><div><span className="eyebrow">Firecrawl tools for developers</span><h1>Firecrawl tools for developers.</h1><p>Firecraw Tools is an independent collection for Firecrawl workflows: use focused web data utilities for public pages, then evaluate a managed service when you need larger crawls, JavaScript rendering, or structured extraction. These web data tools keep small jobs quick and inspectable.</p><div className="hero-actions"><AffiliateButton placement="inline">Try Firecrawl</AffiliateButton><Link className="button secondary" href="/tools/url-to-markdown">Try a free tool</Link></div><p className="tool-help">Sponsored link · Firecraw Tools may earn a commission.</p></div><div className="hero-terminal"><div className="terminal-top"><span className="terminal-dot"/><span className="terminal-dot"/><span className="terminal-dot"/></div><div><span style={{color:"#ff9f87"}}>url</span> = https://example.com/docs</div><div style={{color:"#93b5ff",marginTop:12}}>→ fetching readable content</div><div style={{color:"#7be0a6",marginTop:12}}>✓ Markdown ready</div><div style={{color:"#93a4b9",marginTop:18}}># Documentation</div><div style={{color:"#93a4b9"}}>A clean, portable result.</div></div></div></section><section className="home-section"><div className="container"><span className="eyebrow">Start with a small job</span><h2>Useful web data tools for Firecrawl workflows.</h2><p className="section-intro">Each web data tool handles one clear task. These web data tools are useful for a first pass; when your project grows to JavaScript pages, batch URLs, or structured extraction, the guides explain what to evaluate next. The web data tools are designed to show the result before you commit to a larger workflow.</p><div className="card-grid">{tools.map(([title, desc, href])=><Link className="card" href={href} key={href}><h3>{title}</h3><p>{desc}</p><span className="text-link">Open tool →</span></Link>)}</div></div></section><section className="home-section"><div className="container prose"><span className="eyebrow">Choose by workflow</span><h2>Start with tools for a Firecrawl workflow.</h2><p>Different jobs need different levels of retrieval. The web data tools here are intentionally small, so you can inspect an output before adding a queue, browser runtime, or recurring job. These web data tools stay focused on one result instead of hiding limits behind a broad promise.</p><div className="card-grid"><div className="card"><h3>One public page</h3><p>Use URL to Markdown or Webpage to Markdown when you know the page you need. Copy the readable result into notes, a prompt, or a documentation draft.</p></div><div className="card"><h3>A small site batch</h3><p>Use Website to Markdown when a sitemap already lists the pages. Select only the URLs you need instead of pretending a limited utility is an unlimited crawler.</p></div><div className="card"><h3>AI discovery context</h3><p>Use the llms.txt generator to create a short, editable directory. It helps you curate important URLs before you decide how to retrieve their content.</p></div></div><h2>Know when a larger workflow is justified.</h2><p>Move beyond these web data tools when you need JavaScript rendering, retries, proxy controls, scheduled crawls, structured extraction, or hundreds of URLs. The <Link href="/firecrawl-api">Firecrawl API guide</Link> explains the managed path, while the <Link href="/firecrawl-self-hosted">self-hosted guide</Link> covers the infrastructure you would own.</p><p>Every utility is designed for public inputs and best-effort output. Do not submit private URLs or secrets. Check the returned Markdown, links, and selected pages before using them in an automated pipeline.</p></div></section><section className="home-section" style={{background:"#eef3f9"}}><div className="container"><span className="eyebrow">Developer guides</span><h2>Understand the Firecrawl workflow.</h2><p className="section-intro">These web data tools pair with independent explainers covering API access, MCP, Python, pricing, self-hosting, and alternatives so you can choose a setup that matches your workload.</p><div className="card-grid"><Link className="card" href="/firecrawl-api"><h3>Firecrawl API</h3><p>See the smallest scrape and crawl examples.</p><span className="text-link">Read the API guide →</span></Link><Link className="card" href="/firecrawl-mcp"><h3>Firecrawl MCP</h3><p>Connect an agent to web access with a clear setup path.</p><span className="text-link">Read the MCP guide →</span></Link><Link className="card" href="/firecrawl-alternatives"><h3>Alternatives by use case</h3><p>Compare managed APIs, open source, search, and actors.</p><span className="text-link">See alternatives →</span></Link></div><div className="stat-strip"><div><strong>One task</strong><span>Every web data tool has a defined input and output.</span></div><div><strong>Clear limits</strong><span>Errors and size limits are shown in the interface.</span></div><div><strong>Independent</strong><span>Firecraw Tools is not the official Firecrawl site.</span></div></div><div style={{marginTop:28}}><AffiliateButton placement="inline">Try Firecrawl for larger workflows</AffiliateButton></div></div></section><div className="container narrow"><FAQSection items={pageFaqs.home}/></div></PageShell>;
}
