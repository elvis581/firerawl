import { ContentPage } from "@/components/ContentPage"; import { CodeBlock } from "@/components/CodeBlock"; import { FirecrawlCard } from "@/components/FirecrawlCard"; import { pageMetadata } from "@/lib/seo";
export const metadata=pageMetadata("Firecrawl API: Scrape and Crawl","A practical Firecrawl API guide with copyable JavaScript and Python examples for scrape, crawl, search, and structured extraction workflows.","/firecrawl-api");
export default function Api(){return <ContentPage breadcrumbs={[{name:"Home",href:"/"},{name:"Firecrawl API",href:"/firecrawl-api"}]} faqKey="api" kicker="Developer implementation" title="Firecrawl API examples for scrape and crawl" description="The Firecrawl API exposes web fetching and crawl workflows behind an API key. Start with one URL, then add crawl or extraction when the job requires it."><div className="prose"><p>First, set the server-side key. Second, send one scrape request. Finally, validate the returned content before storing it.</p><ol><li>Set <code>FIRECRAWL_API_KEY</code> in the server environment.</li><li>Send one request for a representative public URL.</li><li>Inspect the response and validate the fields your application needs.</li></ol><h2>Minimal JavaScript scrape</h2><CodeBlock label="JavaScript">{`const response = await fetch("https://api.firecrawl.dev/v1/scrape", {\n  method: "POST",\n  headers: {\n    "Authorization": \`Bearer \${process.env.FIRECRAWL_API_KEY}\`,\n    "Content-Type": "application/json"\n  },\n  body: JSON.stringify({ url: "https://example.com", formats: ["markdown"] })\n});\n\nconst result = await response.json();`}</CodeBlock><h2>Python pattern</h2><CodeBlock label="Python">{`import os\nfrom firecrawl import FirecrawlApp\n\napp = FirecrawlApp(api_key=os.environ["FIRECRAWL_API_KEY"])\nresult = app.scrape_url("https://example.com", params={"formats": ["markdown"]})\nprint(result)`}</CodeBlock><h2>Scrape, crawl, search, extraction</h2><p><strong>Scrape</strong> fits one known URL. <strong>Crawl</strong> fits a site section. <strong>Search</strong> helps when you need discovery before fetching. <strong>Structured extraction</strong> fits records that should follow a schema, such as product names and prices.</p><h2>Firecrawl API production checks</h2><ul><li>Keep the API key in a server environment variable.</li><li>Set timeouts and retry rules in your own worker.</li><li>Log the URL and request id without logging secrets.</li><li>Validate extracted data before storing or showing it.</li></ul></div><FirecrawlCard placement="inline">If you need a managed endpoint for more than a one-page experiment, review the current API access and usage terms. Firecrawl may earn a commission from this link.</FirecrawlCard></ContentPage>}
