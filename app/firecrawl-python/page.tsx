import { ContentPage } from "@/components/ContentPage"; import { CodeBlock } from "@/components/CodeBlock"; import { FirecrawlCard } from "@/components/FirecrawlCard"; import { pageMetadata } from "@/lib/seo";
export const metadata=pageMetadata("Firecrawl Python SDK Guide","Learn the Firecrawl Python SDK setup, API key handling, scrape and crawl examples, extraction patterns, and error handling for a small worker.","/firecrawl-python");
export default function Python(){return <ContentPage breadcrumbs={[{name:"Home",href:"/"},{name:"Firecrawl Python",href:"/firecrawl-python"}]} faqKey="python" kicker="Python implementation" title="Firecrawl Python examples" description="Use the Python SDK when your application already has a Python worker, notebook, or data pipeline. Keep the first test small and inspect the returned shape."><div className="prose"><h2>Install the Firecrawl Python package</h2><CodeBlock label="shell">{`python -m pip install firecrawl-py\nexport FIRECRAWL_API_KEY="your_key_here"`}</CodeBlock><h2>Scrape one page</h2><CodeBlock label="Python">{`import os\nfrom firecrawl import FirecrawlApp\n\napp = FirecrawlApp(api_key=os.environ["FIRECRAWL_API_KEY"])\npage = app.scrape_url(\n    "https://example.com",\n    params={"formats": ["markdown"]},\n)\nprint(page)`}</CodeBlock><h2>Crawl a site section</h2><CodeBlock label="Python">{`job = app.crawl_url(\n    "https://example.com/docs",\n    params={"limit": 10, "scrapeOptions": {"formats": ["markdown"]}},\n)\nprint(job)`}</CodeBlock><h2>Error handling</h2><p>Wrap requests in your application’s timeout and retry policy. Check for authentication, quota, network, and empty-content errors separately so a failed page does not silently become a successful empty record.</p><h2>Keep the Firecrawl Python SDK boundary small</h2><p>Put the Firecrawl call behind one service function. That makes it easier to switch between a local parser, a mock in tests, and a managed crawler as requirements change.</p></div><FirecrawlCard placement="inline">If your Python worker is growing into scheduled crawls, compare the operational cost of maintaining fetches, queues, and browser rendering with a managed API. Firecrawl may earn a commission from this link.</FirecrawlCard></ContentPage>}
