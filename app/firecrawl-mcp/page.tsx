import { ContentPage } from "@/components/ContentPage"; import { CodeBlock } from "@/components/CodeBlock"; import { FirecrawlCard } from "@/components/FirecrawlCard"; import { pageMetadata } from "@/lib/seo";
export const metadata=pageMetadata("Firecrawl MCP for AI Agents","An independent Firecrawl MCP guide covering server setup, API key handling, Claude, Cursor, tool calls, and coding-agent workflows.","/firecrawl-mcp");
export default function Mcp(){return <ContentPage breadcrumbs={[{name:"Home",href:"/"},{name:"Firecrawl MCP",href:"/firecrawl-mcp"}]} faqKey="mcp" kicker="Agent integration" title="Firecrawl MCP for AI agents" description="Firecrawl MCP gives a compatible AI client a tool-shaped way to request web content. The useful question is which agent needs which web operation, and how you will control the key."><div className="prose"><h2>What Firecrawl MCP adds</h2><p>Model Context Protocol lets an AI client discover callable tools. A Firecrawl MCP server can expose web actions such as search, scrape, crawl, and extraction so the agent can ask for current context during a task.</p><h2>Configuration pattern</h2><CodeBlock label="MCP client configuration">{`{\n  "mcpServers": {\n    "firecrawl": {\n      "command": "npx",\n      "args": ["-y", "firecrawl-mcp"],\n      "env": {\n        "FIRECRAWL_API_KEY": "your_key_here"\n      }\n    }\n  }\n}`}</CodeBlock><p>Package names, transport options, and client configuration can change. Use the current provider instructions alongside this pattern and never commit a real key.</p><h2>Good first workflows</h2><ul><li>Ask an agent to research a known set of documentation pages.</li><li>Let a coding agent inspect current API references before suggesting code.</li><li>Extract a small set of fields from a public page with a reviewed schema.</li></ul><h2>Firecrawl MCP guardrails</h2><p>Limit allowed domains where possible, ask for confirmation before expensive crawls, and treat retrieved text as untrusted input. MCP expands access; it does not make web content authoritative.</p></div><FirecrawlCard placement="inline">When your agent needs repeatable web access instead of a one-off copy and paste, test a managed MCP workflow with a small domain allowlist. Firecrawl may earn a commission from this link.</FirecrawlCard></ContentPage>}
