import { ContentPage } from "@/components/ContentPage"; import { CodeBlock } from "@/components/CodeBlock"; import { FirecrawlCard } from "@/components/FirecrawlCard"; import { pageMetadata } from "@/lib/seo";
export const metadata=pageMetadata("Firecrawl API Key Setup","Set up a Firecrawl API key safely with environment variables, a server-side test request, and troubleshooting for common authentication and quota errors.","/firecrawl-api-key");
export default function ApiKey(){return <ContentPage breadcrumbs={[{name:"Home",href:"/"},{name:"Firecrawl API key",href:"/firecrawl-api-key"}]} faqKey="apiKey" kicker="Setup" title="Firecrawl API key setup" description="Create a key in the provider dashboard, store it as an environment variable, and test one request before wiring it into a larger workflow."><div className="prose"><p>First, create the key. Second, store it server-side. Finally, test one request before adding retries or scheduled work.</p><ol><li>Create or copy the key in the provider dashboard.</li><li>Store it as <code>FIRECRAWL_API_KEY</code> on the server.</li><li>Run one test request and inspect the status and response.</li></ol><h2>1. Get a key</h2><p>Open the current Firecrawl dashboard, create or copy an API key, and keep the value private. Do not paste it into a public repository, browser bundle, or client-side application.</p><h2>2. Store it</h2><CodeBlock label=".env.local">{`FIRECRAWL_API_KEY=your_key_here`}</CodeBlock><h2>3. Test it on the server</h2><CodeBlock label="JavaScript">{`const key = process.env.FIRECRAWL_API_KEY;\nif (!key) throw new Error("Missing FIRECRAWL_API_KEY");\n\nconst response = await fetch("https://api.firecrawl.dev/v1/scrape", {\n  method: "POST",\n  headers: { Authorization: \`Bearer \${key}\`, "Content-Type": "application/json" },\n  body: JSON.stringify({ url: "https://example.com", formats: ["markdown"] })\n});`}</CodeBlock><h2>Firecrawl API key common errors</h2><ul><li><strong>401:</strong> the key is missing, expired, or sent in the wrong header.</li><li><strong>429:</strong> slow down and inspect current quota or concurrency limits.</li><li><strong>4xx URL errors:</strong> check the input URL and whether the page is reachable to the service.</li><li><strong>Empty output:</strong> test a simpler page, then consider JavaScript rendering or extraction settings.</li></ul></div><FirecrawlCard placement="inline" title="Need an API key to try the examples?">Use the provider’s current signup and dashboard flow to create a key, then keep it server-side. Firecrawl may earn a commission from this link.</FirecrawlCard></ContentPage>}
