import { ToolLayout } from "@/components/ToolLayout"; import { LlmsTxtTool } from "@/components/LlmsTxtTool"; import { pageMetadata } from "@/lib/seo";
export const metadata=pageMetadata("llms.txt Generator","Generate a starter llms.txt file from a public sitemap, select important URLs, review the list, and copy or download the result.","/tools/llms-txt-generator");
export default function Llms(){return <ToolLayout breadcrumbs={[{name:"Home",href:"/"},{name:"Tools",href:"/tools"},{name:"llms.txt generator",href:"/tools/llms-txt-generator"}]} faqKey="llms" kicker="AI discovery utility" title="llms.txt generator" description="Find important pages in a public sitemap and create a small, editable llms.txt starter file."><LlmsTxtTool/><div className="prose section"><h2>What does an llms.txt generator produce?</h2><p>llms.txt is a proposed convention for giving AI systems a curated list of useful site pages. It is a plain-text directory that you can review and edit.</p><h2>Choose pages with a clear purpose</h2><p>Start with documentation, product explanations, pricing context, or other pages that represent the site accurately. A short list is easier to maintain than a sitemap dump, and a human should remove thin, private, duplicate, or outdated URLs before publishing.</p><h2>Where does it go?</h2><p>After reviewing the output, publish it at the root of your site if that matches your deployment plan. It is separate from robots.txt and sitemap.xml: those files serve different discovery and crawler-control purposes.</p><h2>Keep the file curated</h2><p>An llms.txt generator works best with a short list of authoritative pages, which is easier to maintain than an automatic dump. If you need clean Markdown for every selected page, evaluate a crawl workflow after the directory is ready.</p></div></ToolLayout>}
