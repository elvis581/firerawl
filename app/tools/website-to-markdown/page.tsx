import { ToolLayout } from "@/components/ToolLayout"; import { WebsiteMarkdownTool } from "@/components/WebsiteMarkdownTool"; import { pageMetadata } from "@/lib/seo";
export const metadata=pageMetadata("Website to Markdown","Discover a public sitemap, select up to 10 pages, and export a focused website-to-Markdown batch for review or documentation.","/tools/website-to-markdown");
export default function Website(){return <ToolLayout breadcrumbs={[{name:"Home",href:"/"},{name:"Tools",href:"/tools"},{name:"Website to Markdown",href:"/tools/website-to-markdown"}]} faqKey="website" kicker="Selected site pages" title="Website to Markdown" description="Enter a domain to discover its sitemap, select up to 10 public pages, and plan a focused Markdown batch."><WebsiteMarkdownTool/><div className="prose section"><h2>How this website to Markdown workflow works</h2><p>Enter a domain, let the tool look for a public sitemap, and select only the pages that belong in your batch. The result combines the selected pages into a reviewable Markdown download, which is useful for a small documentation set, a content audit, or a focused research pass.</p><h2>Why this website to Markdown tool is limited</h2><p>A useful website to Markdown batch should be inspectable. This tool discovers sitemap URLs and lets you select pages; it does not run an unlimited crawler or proxy network. Use it for planning and small batches, and check the selected URLs before conversion begins.</p><h2>What can cause a partial result?</h2><p>A site may not expose a readable sitemap, may return private or blocked pages, or may rely on browser-rendered content that a simple fetch cannot see. Selecting fewer representative pages makes failures easier to diagnose than starting with an entire site.</p><h2>Website to Markdown for a full website crawl</h2><p>Large sites need concurrency, retries, JavaScript rendering, and queue management. Read about the <a href="/firecrawl-api">Firecrawl API</a> or <a href="/firecrawl-self-hosted">self-hosting tradeoffs</a> when that is your actual requirement.</p></div></ToolLayout>}
