import { ToolLayout } from "@/components/ToolLayout"; import { UrlExtractorTool } from "@/components/UrlExtractorTool"; import { pageMetadata } from "@/lib/seo";
export const metadata=pageMetadata("URL extractor","Extract links from a public webpage or pasted HTML, separate internal and external URLs, review totals, and export a CSV for analysis.","/tools/url-extractor");
export default function Extractor(){return <ToolLayout breadcrumbs={[{name:"Home",href:"/"},{name:"Tools",href:"/tools"},{name:"URL extractor",href:"/tools/url-extractor"}]} faqKey="extractor" kicker="Link discovery tool" title="URL extractor" description="Fetch a public page or paste HTML to list its links. Filter the result mentally by the internal and external totals, then export a CSV."><UrlExtractorTool/><div className="prose section"><h2>What this URL extractor returns</h2><p>The tool resolves relative links when you fetch a URL, counts total links, and separates same-host links from other destinations. It does not crawl each result or validate that every link is live.</p><h2>Use the URL extractor for a quick inventory</h2><p>Fetch a public page when you want to inspect its navigation, resource references, or outbound destinations. Paste HTML instead when you already have a saved response or want to test a snippet without making another request. The TXT export is useful for a simple list; CSV adds an internal or external label for spreadsheet review.</p><h2>How to interpret the totals</h2><p>Internal means the link resolves to the same host as the fetched page. External means it points elsewhere. A repeated navigation link can increase the total, and a listed URL can still be broken or blocked later. Treat the output as discovery data, not a live-link audit. This URL extractor is a first-pass inventory, not a crawler. Use this URL extractor before a crawl when you need to understand the link shape first.</p><h2>When to use a crawler</h2><p>If you need content from every discovered URL, link depth, retries, or JavaScript pages, a link list is only the first step. Compare a managed crawling workflow through the <a href="/firecrawl">Firecrawl guide</a>.</p></div></ToolLayout>}
