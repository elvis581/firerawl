"use client";
import { useState } from "react"; import { AffiliateButton } from "./AffiliateButton";
export function UrlExtractorTool(){const[url,setUrl]=useState('');const[html,setHtml]=useState('');const[tab,setTab]=useState<'url'|'html'>('url');const[error,setError]=useState('');const[result,setResult]=useState<{links:string[];internal:string[];external:string[]}|null>(null);const[loading,setLoading]=useState(false);const[copied,setCopied]=useState(false);async function run(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');setCopied(false);try{const r=await fetch('/api/extract-links',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(tab==='url'?{url}:{html})});const d=await r.json();if(!r.ok)throw new Error(d.error);setResult(d)}catch(e){setError(e instanceof Error?e.message:'Unable to extract links.')}finally{setLoading(false)}}async function copy(){if(!result)return;await navigator.clipboard.writeText(result.links.join('\n'));setCopied(true)}function save(content:string,name:string,type:string){const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([content],{type}));a.download=name;a.click();URL.revokeObjectURL(a.href)}function downloadTxt(){if(result)save(result.links.join('\n'),'urls.txt','text/plain')}function downloadCsv(){if(result){const rows=['url,type',...result.links.map(x=>`${JSON.stringify(x)},${result.internal.includes(x)?'internal':'external'}`)];save(rows.join('\n'),'urls.csv','text/csv')}}return <div className="tool-panel"><div className="result-actions" style={{marginBottom:16}}><button type="button" className={`button ${tab==='url'?'primary':'secondary'}`} onClick={()=>setTab('url')}>Fetch a URL</button><button type="button" className={`button ${tab==='html'?'primary':'secondary'}`} onClick={()=>setTab('html')}>Paste HTML</button></div><form onSubmit={run}>{tab==='url'?<><label className="field-label" htmlFor="extract-url">Page URL</label><input className="url-input" id="extract-url" value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://example.com" type="url" required/></>:<><label className="field-label" htmlFor="extract-html">HTML</label><textarea className="url-input" id="extract-html" value={html} onChange={e=>setHtml(e.target.value)} placeholder="<a href=...>" rows={7} required/></>}<button className="button primary" style={{marginTop:12}} disabled={loading}>{loading?'Extracting…':'Extract URLs'}</button></form>{error&&<div className="tool-error" role="alert">{error}</div>}{result&&<div className="result-panel"><div className="result-head"><div><h2>Extracted links</h2><div className="stats"><span><strong>{result.links.length}</strong> total</span><span><strong>{result.internal.length}</strong> internal</span><span><strong>{result.external.length}</strong> external</span></div></div><div className="result-actions"><button className="button secondary" onClick={copy}>{copied?'Copied':'Copy'}</button><button className="button secondary" onClick={downloadTxt}>Export TXT</button><button className="button secondary" onClick={downloadCsv}>Export CSV</button></div></div><pre className="markdown-output">{result.links.join('\n')}</pre><div className="affiliate-card"><div><span className="eyebrow">Next step</span><h3>Need to crawl and extract content too?</h3><p>Link lists are useful for discovery. A crawl adds page retrieval and content output.</p></div><AffiliateButton placement="tool-result">Explore Firecrawl</AffiliateButton></div></div>}</div>}
